{
  "name": "boladas",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "./scripts/dev.sh",
    "dev:services": "concurrently --kill-others --prefix '[{name}]' --names 'api,app' --prefix-colors 'yellow,cyan' 'pnpm dev:api' 'pnpm dev:app'",
    "dev:api": "pnpm -C apps/api dev",
    "dev:app": "pnpm -C apps/boladas dev",
    "build": "pnpm -C apps/boladas build && pnpm -C apps/api build",
    "lint": "pnpm -C apps/boladas lint && pnpm -C apps/api lint",
    "typecheck": "pnpm -C apps/boladas typecheck && pnpm -C apps/api typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
